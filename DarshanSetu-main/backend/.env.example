# Server Configuration
PORT=5000
JWT_SECRET=darshansetu_jwt_super_secret_key_2026

# Database (PostgreSQL via Docker or local/cloud Postgres)
# Default Docker port from docker-compose.yml is 5433
DATABASE_URL="postgresql://kshem_admin:kshem_password@localhost:5433/darshansetu?schema=public"

# Cache & Ingestion (Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL=redis://localhost:6379

# Telemetry Broker (Mosquitto MQTT)
MQTT_BROKER_URL=mqtt://localhost:1883
